{% extends 'base.html.twig' %}

{% block title %}Change ton mot de passe{% endblock %}

{% block body %}
    <h1>Change ton mot de passe</h1>
<div class="mb-3">
    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}</div>
        <button class="btn btn-primary">Valider</button>
    {{ form_end(resetForm) }}
{% endblock %}
