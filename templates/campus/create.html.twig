{% extends 'base.html.twig' %}

{% block title %} {{ parent()  }} | Création Campus{% endblock %}

{% block body %}
    <h2>Création d'un nouveau campus</h2>
    <div class="sortie-create mt-5 col-6">
        <div class="sortie-column">
            {{ form_start(campusForm) }}
            <div class="form_widget ">{{ form_widget(campusForm) }}
            <button type="submit" class="btn btn-light mt-5">
               Ajouter
            </button>
            {{ form_end(campusForm) }}
        </div>
    </div>
{% endblock %}